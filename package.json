{
  "name": "eslint-plugin-ember-data-sync-relationships",
  "version": "0.0.0",
  "description": "Ensures Ember Data relationships are sync (async: false)",
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "author": "Sam Selikoff",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha tests --recursive",
    "test:debug": "node --inspect-brk ./node_modules/mocha/bin/mocha tests --recursive"
  },
  "dependencies": {
    "requireindex": "~1.1.0"
  },
  "devDependencies": {
    "eslint": "~3.9.1",
    "mocha": "^3.1.2"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "license": "ISC"
}
